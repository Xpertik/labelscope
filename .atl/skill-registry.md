# Skill Registry — labelscope

Generated by `sdd-init`. This is infrastructure, not an SDD artifact.

## User-level skills

- **find-skills** — `/home/jhonatan/.claude/skills/find-skills/SKILL.md`
  Trigger: Discover and install agent skills when user asks "how do I do X", "find a skill for X", "is there a skill that can...".

- **skill-creator** — `/home/jhonatan/.claude/skills/skill-creator/SKILL.md`
  Trigger: When user asks to create a new skill, add agent instructions, or document patterns for AI.

- **go-testing** — `/home/jhonatan/.claude/skills/go-testing/SKILL.md`
  Trigger: Go testing (not relevant for this Python project — retained for completeness).

## Project conventions

- **contexto.md** — `/home/jhonatan/trunk/xpertik/labelscope/contexto.md`
  Project spec: architecture, EPL2 command priorities, stack, decisions, fixtures workflow. Source of truth for design decisions.

No `CLAUDE.md`, `AGENTS.md`, `agents.md`, `.cursorrules`, or `GEMINI.md` detected at the project root.

## Notes for sub-agents

When launching a sub-agent for this project, pre-resolve the skill path and pass it:
```
SKILL: Load `<path>` before starting.
```
Sub-agents should NOT re-scan the registry themselves.
